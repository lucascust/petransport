# Database connection
MONGODB_URI=mongodb://localhost:27017/

# Application settings
SECRET_KEY=your-secret-key-here
ADMIN_PASSWORD=admin123
GOOGLE_MAPS_API_KEY=your-google-maps-api-key

# Firebase configuration
# Option 1: Store your service account JSON as an environment variable
# This should be the entire JSON as a string (escaped properly)
FIREBASE_SERVICE_ACCOUNT={"type":"service_account","project_id":"pettransport-b01b8",...}

# Option 2: Use a service-account.json file in the project root
# In this case, leave FIREBASE_SERVICE_ACCOUNT blank 